# 三阶段 Skill 与工作流程迁移范围

整理日期：2026-09-07。基线是私有仓库冻结提交 `05aa9a2671d2319964b34ff1e2ecd10a17eea4b1`，与当日生产工作区的活动文件逐项比较；生产工作区有未提交改动，因此不能只用其 Git HEAD 代表实际版本。

此次采用提取副本，不删除、不改写原生产工作区，也不切换本机全局 Skill。文件对照及 SHA-256 见 [迁移清单](migration-manifest.json)，其目标快照固定为迁移提交 `7964c6434f27a9b700ff4855dff2ee7bff97033f`，不代表后续 README 与素材修改后的文件哈希；清单只保存相对路径与文件身份，不含私人工作区路径。

## 活动流程全量纳入

| 内容 | 处理 |
| --- | --- |
| `srt-broll-producer` | 新增完整 Skill、agents 元数据和语义拆镜 reference。 |
| `broll-storyboard-producer` | 新增完整 Skill、agents 元数据和四格契约 reference。 |
| `h3-video-runtime` | 原仓库已经具备完整目录；保留流程，通用化称谓并补齐恢复文件树。 |
| `imagegen` / Codex Imagine | 记录为外部宿主依赖，不复制系统 Skill，不声称 pip 安装可以提供生图能力。 |
| `broll_video/`、`schemas/`、原有 `tests_broll/`、`pyproject.toml`、`uv.lock` | 原仓库与活动生产源逐文件一致；保留完整实现，没有重写生产代码。 |
| 四份生产配置 | 原仓库已具备控制、计划价、八模型清单和栈指纹；保留事实与已批准版本。 |
| `work/TASK_TEMPLATE.json` | 提取到 `templates/TASK_TEMPLATE.json`，避免与被忽略的私人 `work/` 混淆；真实任务仍由 CLI 初始化。 |
| 三阶段流程 / 本机安装 / 恢复说明 | 新增总流程，修正不存在的旧备份命令，说明 clone 安装、宿主生图和 AutoDL 支持范围。 |
| 部署材料 | 保留小型节点、安装/启动/下载/验证工具；补齐 SageAttention 固定版本和已有读取补丁的安装。 |
| 收据与归档 | 历史盘点只保留脱敏副本，注明原始证据哈希；源码包去掉 51 项 AppleDouble 记录及扩展元数据，33 个有效文件字节与原包一致。 |

另新增合成 `examples/demo.srt`，仅检查安装与任务初始化，不将其描述为真实生成样例。

## 不进入活动发行内容

| 原项目内容 | 判断与去向 |
| --- | --- |
| `legacy/commerce-skills/` 的商品研究、广告策略、参考广告分析、旧提示词编译、音频及后期 Skill | 已停用的电商流程，根项目明确禁止新任务使用；保留在原生产项目历史区。 |
| `videos/` 内独立 HyperFrames 成片项目及其 AGENTS 路由清单 | 不属于根目录三阶段 B-roll 生成链；列出某 Skill 不等于实际调用过，不据此打包整套 HyperFrames。 |
| 日期开头的 FDE 镜头、故事板与 bundle 脚本 | 绑定具体文案、字体、素材和作者路径的个例工具，不是可供新任务调用的通用入口；规则已由活动 Skill 与编译器承接。 |
| 两份生产复盘、聊天和任务记录 | 通用经验已落到 Skill / 三阶段流程，原始记录留在私人项目。 |
| 旧 `build_h3_control_plane_backup.py` | 路径依赖原工作区且漏前两阶段；本仓库用 `git archive` 导出已提交全量材料，不迁旧缺件脚本。 |
| `config/autodl.local.example.json`、`.env.example` | 前者仍为旧 Token 模式，后者仅为空配置说明；统一使用当前 `install-credentials` 入口，避免引导用户填旧字段。 |
| `secrets/`、真实 `.env`、`work/tasks/`、输入素材、生成提示词与结果、运行日志、虚拟环境、模型和 NVIDIA wheel | 保留私人目录或在部署时下载，不进入此次迁移。 |

## 未来公开前的实际剩余事项

- 自研内容的发行许可，以及固定模型版本的适用许可核验，见 [第三方记录](../THIRD_PARTY_NOTICES.md)。
- 新 GPU 上从零安装、完整栈验证、最小生成与恢复验收。本次只在本机验证；未产生付费生成。
- 当前是 AutoDL 固定布局，不承诺通用云、多种 GPU 或任意图片后端。
- 本次未重写历史提交。旧私有冻结提交仍包含原机器挂载标识和原始归档元数据；若未来公开，宜从已审查的新源码快照建立干净公开历史，或先另行审查与清理历史，不能直接把“当前文件已脱敏”当作“全部历史可公开”。
