# H3Loom 发行候选核验

核验日期：2026-09-12。首个公开版本为 `v0.2.0rc1`，目标仓库 [erduo1998-cell/h3loom](https://github.com/erduo1998-cell/h3loom)，默认分支 `main`。本页说明首版的验证范围，远端执行结果见 [GitHub Actions](https://github.com/erduo1998-cell/h3loom/actions) 和 [Release](https://github.com/erduo1998-cell/h3loom/releases/tag/v0.2.0rc1)。

## 已完成的本地准备

- 从冻结私有仓库导出文件建立独立候选，没有复制旧 `.git` 历史。原私有仓库与活动生产目录未修改。
- 对外名称缩为 H3Loom；包名 `h3loom`，版本 `0.2.0rc1`，原命令 `broll-video` / `broll-autodl` 保持兼容。名称检索未发现明确同名 GitHub 仓库；这不保证搜索排名或商标可用性。
- 三个 Skill 及相关说明与当前生产来源对齐，九个文件逐字节身份见 [对齐记录](production-alignment.json)。
- 云端依赖改为生产收据的固定版本约束，包含 SageAttention 1.0.6；原上游源码提交和模型哈希保持固定。
- 修复运行时依赖旧机器的时间戳模型收据路径的问题。下载器实际核验全部模型后写入稳定收据；损坏文件不能沿用旧成功标记，已验证文件不必重下。
- 补充第三方许可固定版本与 SHA-256、贡献和安全说明、不调用云端生成的 CI 定义、相对链接及发行文件检查。
- 五语 README 更新名称、流程图、候选状态和许可提示；韩语文档明确语言不代表韩国地区使用许可。保留原咨询入口与成本口径。

## 已执行的验证

| 验证 | 结果及边界 |
| --- | --- |
| 当前候选单元测试 | 85/85 通过，均为本机逻辑测试。 |
| 三个 Skill 官方校验器 | 全部通过；校验器的 PyYAML 在临时工具环境安装，未改变产品依赖。 |
| 干净文件快照 | 在新目录重新离线安装锁定依赖，85/85 测试及发行检查通过；合成 SRT 的 estimate-only 任务创建成功。 |
| CLI 入口 | 两个 help 入口正常；合成任务 next 正确停在 needs_visual_lock，要求两类参考。未触发付费调用。 |
| 打包 | Python sdist 与 wheel 构建通过；完整使用仍要求 clone，因为根目录 Skill、schema、config 不随 wheel 安装。 |
| 部署静态检查 | Shell 语法、生产清单一致性、云端 pins 与生产收据一致性通过。 |
| 秘密扫描 | Gitleaks 8.30.1 对本轮文件快照未发现泄漏；它不是穷尽性安全保证，也不是旧私有历史扫描。 |
| README 本地渲染 | 中文 390px 手机、英文 1280px 桌面检查通过，本地图片与徽章加载，无页面横向溢出。 |
| 介绍视频技术检查 | 本地代码渲染 720 帧并逐项验证 PNG，完整 MP4 解码通过；视频/音频均为 24 秒，1080p/30fps，音频峰值 -6.5 dB，未削波。连续片段检查与字体修复完成；所有者已于 2026-09-12 确认发布此重制版。 |

## 首次公开决定与边界

所有者于 2026-09-12 确认公开发布，并选择 Apache-2.0。自有代码、三个 Skill 和文档文字的许可已写入根目录 [LICENSE](../LICENSE) 与 [NOTICE](../NOTICE)，第三方条款独立保留。五语 README 使用实际公开仓库 URL，介绍视频采用获批的本地代码三维重制版。

本次开源以 **v0.2.0rc1 预发行版**发布完整源码、三阶段 Skill、部署材料和介绍素材。独立仓库只推送清理后的文件历史；原私有仓库保持私有，活动生产目录保持原状。

**尚未完成：新 AutoDL GPU 的从零安装和端到端实机验收。** 后续需要在指定 GPU 上检查安装、节点、4K VFX smoke、经授权的最小 H3 生成、下载、恢复和关机，并记录实际支出。本次发布未使用生产 GPU，也未调用付费生成；本机与 CI 测试不能替代这些 GPU 证据。完成前不声明新实例的完整重建已经通过。
