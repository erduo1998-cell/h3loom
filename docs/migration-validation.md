# 三阶段迁移验证记录

日期：2026-09-07。验证对象是本仓库迁移候选；未调用图片工具、云端 SSH、模型下载或付费生成。

## 已完成

- 基线比较：64 个活动生产文件与目标逐项核对，其中 53 个字节相同、11 个为脱敏或交接适配；完整对照见 `migration-manifest.json`。生产代码 `broll_video/`、schema 与四份生产配置保持源文件字节一致。
- 本机运行：Python 3.12.13，通过 `uv sync --frozen` 安装；两个 CLI 的帮助命令可启动。
- 测试：原有 74 项及新增 7 项，共 81 项通过；新增检查覆盖部署补丁与盘点隐私。运行时的批准、提示词、预算、恢复及下载测试使用合成数据或模拟传输。
- 三个 Skill 均通过宿主 `skill-creator/scripts/quick_validate.py`；入口、agents 元数据和相对引用已检查。
- 独立新接手 Agent 在临时目录使用合成 SRT 创建任务：初始 `estimate_only`、三处视觉批准 `pending`，输入字节完整保留；`next` 正确返回 `needs_visual_lock` 和 `srt-broll-producer`。未批准直接编译 H3 被拒绝。未把缺少参考的任务假装做完。
- 部署补丁：读取官方 ComfyUI 冻结提交的三个原始源码文件，在本地临时 Git 仓库执行 4 项测试，验证首次应用、再次运行不变和异常内容拒绝。未执行整机安装。
- H3 Easy 归档：去除 51 个元数据条目，33 个有效文件字节与旧包一致；包内校验脚本通过，安装器已使用新归档 SHA-256。
- 历史盘点收据：挂载设备来源脱敏，保留文件系统与挂载点；原始收据哈希与脱敏标记保留，模型及栈比对结论未改写。
- 当前候选文本做私人路径、凭证形状与实际连接端点筛查；连接域名命中只来自已有合成测试夹具。没有将真实任务、素材、密码或大型模型带入新文件。
- `bash -n` 与 `git diff --check` 通过。

## 安装副本

已把候选的 Git 暂存文件导出到全新临时目录，重新执行 `uv sync --frozen`：安装成功，两个 CLI 帮助命令成功，合成任务创建及下一阶段路由成功，81 项测试再次全部通过。该验证不复用原生产目录、私人配置或原项目虚拟环境。

## 不代表已验证

- 未重新生成真实图片或视频，不对新素材的文字、声音或审美作保证。
- 未在新 GPU 上从零部署；SageAttention/CUDA、完整节点栈、4K 生成、下载/恢复和关机仍需实机验收。
- 不承诺任意云、任意 GPU、任意生图后端或 Windows 安装；当前记录只覆盖本机 macOS 控制端和原有 AutoDL 固定布局。
- 未决定整仓发行许可，未完成模型固定版本的许可核验。
- 未审查或改写全部 Git 历史；公开前需要处理旧私有提交保留的机器标识和原包元数据。
